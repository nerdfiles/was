// Generated by CoffeeScript 1.11.1

/*
@fileOverview ./src/assets/store.key.coffee
@description
 */
var KeyStore, _;

_ = require('lodash');


/*
  Attributes:
    has
    get
    all
 */

KeyStore = (function() {
  function KeyStore(store) {
    this.store = store;
  }

  KeyStore.prototype.has = function(key) {
    return this.store.hasOwnProperty(key);
  };

  KeyStore.prototype.get = function(key, defaultVal) {
    if (!defaultVal) {
      defaultVal = null;
    }
    if (this.store.hasOwnProperty(key)) {
      return this.store[key];
    } else {
      return defaultVal;
    }
  };

  KeyStore.prototype.all = function() {
    return _.assign({}, this.store);
  };

  return KeyStore;

})();

export default KeyStore;
